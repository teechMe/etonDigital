{
  "devDependencies": {
    "@types/node": "^22.13.13",
    "cypress": "^14.1.0",
    "cypress-if": "^1.13.2",
    "cypress-xpath": "^2.0.1",
    "mocha-junit-reporter": "^2.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "test": "cypress run --reporter mocha-junit-reporter --reporter-options mochaFile=cypress/results/results.xml",
    "test:ci": "npm run test -- --headless --browser chrome"
  }
}